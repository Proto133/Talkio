<section class="comments">
  {{#each this}}
  <section class="card comment">
    <div class="comInfo">
      {{User.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</section>